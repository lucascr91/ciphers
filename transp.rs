struct Columnar {
    key: i32,
}

impl Columnar {
    fn encrypt(&self, message: &str) -> String {
        let mut cipher_text = !vec['', self.key];

        for column in 0..key {
            current_index= column;

            while current_index< message.len() {
                cipher_text[column]
            }
        }
    }
}